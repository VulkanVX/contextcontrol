<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ContextControl.Workbench.Views.ThemeSettingsWindowParts.AppearanceSettingsPage"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:ContextControl.Workbench.Controls"
    xmlns:vm="clr-namespace:ContextControl.Workbench.ViewModels"
>
          <Grid x:Name="AppearancePageRoot" RowDefinitions="Auto,Auto,Auto,Auto,*" RowSpacing="6" ClipToBounds="True">
            <Grid ColumnDefinitions="*,*,*,*,*,*" ColumnSpacing="8">
              <StackPanel Spacing="3">
                <TextBlock Classes="field-label" Text="Skin" />
                <ComboBox
                    x:Name="SkinPicker"
                    Classes="theme-picker"
                    ItemsSource="{Binding Skins}"
                    SelectedItem="{Binding SelectedSkin, Mode=TwoWay}"
                    DropDownClosed="OnOptionPickerDropDownClosed"
                    SelectionChanged="OnSkinSelectionChanged">
                  <ComboBox.SelectionBoxItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ThemeOptionViewModel}">
                      <TextBlock Text="{Binding Name}" FontSize="10.5" Foreground="{DynamicResource TextPrimaryBrush}" />
                    </DataTemplate>
                  </ComboBox.SelectionBoxItemTemplate>
                  <ComboBox.ItemContainerTheme>
                    <ControlTheme TargetType="ComboBoxItem">
                      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </ControlTheme>
                  </ComboBox.ItemContainerTheme>
                  <ComboBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ThemeOptionViewModel}">
                      <Border
                          Padding="1,0"
                          Background="Transparent"
                          HorizontalAlignment="Stretch">
                        <StackPanel Spacing="0">
                          <TextBlock Text="{Binding Name}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" />
                          <TextBlock
                              Text="{Binding Description}"
                              FontSize="10"
                              Foreground="{DynamicResource TextMutedBrush}" />
                        </StackPanel>
                      </Border>
                    </DataTemplate>
                  </ComboBox.ItemTemplate>
                </ComboBox>
              </StackPanel>

              <StackPanel Grid.Column="1" Spacing="3">
                <TextBlock Classes="field-label" Text="Theme" />
                <ComboBox
                    x:Name="ThemePicker"
                    Classes="theme-picker"
                    ItemsSource="{Binding Themes}"
                    SelectedItem="{Binding SelectedTheme, Mode=TwoWay}"
                    IsEnabled="{Binding AppearanceOptionsEnabled}"
                    DropDownClosed="OnOptionPickerDropDownClosed"
                    SelectionChanged="OnThemeSelectionChanged">
                  <ComboBox.SelectionBoxItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ThemeOptionViewModel}">
                      <TextBlock Text="{Binding DisplayName}" FontSize="10.5" Foreground="{DynamicResource TextPrimaryBrush}" />
                    </DataTemplate>
                  </ComboBox.SelectionBoxItemTemplate>
                  <ComboBox.ItemContainerTheme>
                    <ControlTheme TargetType="ComboBoxItem">
                      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </ControlTheme>
                  </ComboBox.ItemContainerTheme>
                  <ComboBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ThemeOptionViewModel}">
                      <Border
                          Padding="1,0"
                          Background="Transparent"
                          HorizontalAlignment="Stretch"
                          PointerEntered="OnThemeOptionPointerEntered"
                          PointerExited="OnThemeOptionPointerExited">
                        <StackPanel Spacing="0">
                          <TextBlock Text="{Binding DisplayName}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" />
                          <TextBlock
                              Text="{Binding Description}"
                              FontSize="10"
                              Foreground="{DynamicResource TextMutedBrush}" />
                        </StackPanel>
                      </Border>
                    </DataTemplate>
                  </ComboBox.ItemTemplate>
                </ComboBox>
              </StackPanel>

              <StackPanel Grid.Column="2" Spacing="3">
                <TextBlock Classes="field-label" Text="Syntax colors" />
                <ComboBox
                    x:Name="SyntaxThemePicker"
                    Classes="theme-picker"
                    ItemsSource="{Binding SyntaxThemes}"
                    SelectedItem="{Binding SelectedSyntaxTheme, Mode=TwoWay}"
                    IsEnabled="{Binding AppearanceOptionsEnabled}"
                    DropDownClosed="OnOptionPickerDropDownClosed"
                    SelectionChanged="OnSyntaxThemeSelectionChanged">
                  <ComboBox.SelectionBoxItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ThemeOptionViewModel}">
                      <TextBlock Text="{Binding Name}" FontSize="10.5" Foreground="{DynamicResource TextPrimaryBrush}" />
                    </DataTemplate>
                  </ComboBox.SelectionBoxItemTemplate>
                  <ComboBox.ItemContainerTheme>
                    <ControlTheme TargetType="ComboBoxItem">
                      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </ControlTheme>
                  </ComboBox.ItemContainerTheme>
                  <ComboBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ThemeOptionViewModel}">
                      <Border
                          Padding="1,0"
                          Background="Transparent"
                          HorizontalAlignment="Stretch"
                          PointerEntered="OnSyntaxThemeOptionPointerEntered"
                          PointerExited="OnSyntaxThemeOptionPointerExited">
                        <StackPanel Spacing="0">
                          <TextBlock Text="{Binding Name}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" />
                          <TextBlock
                              Text="{Binding Description}"
                              FontSize="10"
                              Foreground="{DynamicResource TextMutedBrush}" />
                        </StackPanel>
                      </Border>
                    </DataTemplate>
                  </ComboBox.ItemTemplate>
                </ComboBox>
              </StackPanel>

              <StackPanel Grid.Column="3" Spacing="3">
                <TextBlock Classes="field-label" Text="Font color" />
                <ComboBox
                    Classes="theme-picker"
                    ItemsSource="{Binding UiFontColorModes}"
                    SelectedItem="{Binding SelectedUiFontColorMode, Mode=TwoWay}">
                  <ComboBox.SelectionBoxItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ThemeOptionViewModel}">
                      <TextBlock Text="{Binding Name}" FontSize="10.5" Foreground="{DynamicResource TextPrimaryBrush}" />
                    </DataTemplate>
                  </ComboBox.SelectionBoxItemTemplate>
                  <ComboBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ThemeOptionViewModel}">
                      <StackPanel Spacing="0">
                        <TextBlock Text="{Binding Name}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" />
                        <TextBlock Text="{Binding Description}" FontSize="10" Foreground="{DynamicResource TextMutedBrush}" />
                      </StackPanel>
                    </DataTemplate>
                  </ComboBox.ItemTemplate>
                </ComboBox>
                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="6" IsVisible="{Binding UseCustomUiFontColor}">
                  <controls:ColorWheelPicker
                      Width="36"
                      Height="36"
                      SelectedColor="{Binding CustomUiFontColor, Mode=TwoWay}" />
                  <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                    <TextBox
                        Classes="rule-add"
                        Text="{Binding CustomUiFontColorText, Mode=TwoWay}"
                        Watermark="#DDE6E8" />
                    <TextBlock Classes="muted" Text="{Binding CustomUiFontColorHex}" TextTrimming="CharacterEllipsis" />
                  </StackPanel>
                </Grid>
              </StackPanel>

              <StackPanel Grid.Column="4" Spacing="3">
                <TextBlock Classes="field-label" Text="Code font" />
                <ComboBox
                    x:Name="CodeFontPicker"
                    Classes="theme-picker"
                    ItemsSource="{Binding CodeFonts}"
                    SelectedItem="{Binding SelectedCodeFont, Mode=TwoWay}"
                    IsEnabled="{Binding AppearanceOptionsEnabled}"
                    DropDownClosed="OnOptionPickerDropDownClosed"
                    SelectionChanged="OnCodeFontSelectionChanged">
                  <ComboBox.SelectionBoxItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ThemeOptionViewModel}">
                      <TextBlock Text="{Binding Name}" FontFamily="{Binding PreviewFontFamily}" FontSize="10.5" Foreground="{DynamicResource TextPrimaryBrush}" />
                    </DataTemplate>
                  </ComboBox.SelectionBoxItemTemplate>
                  <ComboBox.ItemContainerTheme>
                    <ControlTheme TargetType="ComboBoxItem">
                      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </ControlTheme>
                  </ComboBox.ItemContainerTheme>
                  <ComboBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ThemeOptionViewModel}">
                      <Border
                          Padding="1,0"
                          Background="Transparent"
                          HorizontalAlignment="Stretch"
                          PointerEntered="OnCodeFontOptionPointerEntered"
                          PointerExited="OnCodeFontOptionPointerExited">
                        <StackPanel Spacing="0">
                          <TextBlock Text="{Binding Name}" FontFamily="{Binding PreviewFontFamily}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" />
                          <TextBlock
                              Text="{Binding Description}"
                              FontSize="10"
                              Foreground="{DynamicResource TextMutedBrush}" />
                        </StackPanel>
                      </Border>
                    </DataTemplate>
                  </ComboBox.ItemTemplate>
                </ComboBox>
              </StackPanel>

              <StackPanel Grid.Column="5" Spacing="3">
                <TextBlock Classes="field-label" Text="UI font" />
                <ComboBox
                    x:Name="UiFontPicker"
                    Classes="theme-picker"
                    ItemsSource="{Binding UiFonts}"
                    SelectedItem="{Binding SelectedUiFont, Mode=TwoWay}"
                    IsEnabled="{Binding AppearanceOptionsEnabled}"
                    DropDownClosed="OnOptionPickerDropDownClosed"
                    SelectionChanged="OnUiFontSelectionChanged">
                  <ComboBox.SelectionBoxItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ThemeOptionViewModel}">
                      <TextBlock Text="{Binding Name}" FontFamily="{Binding PreviewFontFamily}" FontSize="10.5" Foreground="{DynamicResource TextPrimaryBrush}" />
                    </DataTemplate>
                  </ComboBox.SelectionBoxItemTemplate>
                  <ComboBox.ItemContainerTheme>
                    <ControlTheme TargetType="ComboBoxItem">
                      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </ControlTheme>
                  </ComboBox.ItemContainerTheme>
                  <ComboBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ThemeOptionViewModel}">
                      <Border
                          Padding="1,0"
                          Background="Transparent"
                          HorizontalAlignment="Stretch">
                        <StackPanel Spacing="0">
                          <TextBlock Text="{Binding Name}" FontFamily="{Binding PreviewFontFamily}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" />
                          <TextBlock
                              Text="{Binding Description}"
                              FontSize="10"
                              Foreground="{DynamicResource TextMutedBrush}" />
                        </StackPanel>
                      </Border>
                    </DataTemplate>
                  </ComboBox.ItemTemplate>
                </ComboBox>
              </StackPanel>
            </Grid>

            <Border Grid.Row="1" Classes="rule-card" Padding="6,4">
              <Grid ColumnDefinitions="*,*,*,*" ColumnSpacing="10">
                <Grid RowDefinitions="Auto,Auto" RowSpacing="2" MinWidth="168">
                  <TextBlock Classes="field-label" Text="UI size" />
                  <Grid Grid.Row="1" ColumnDefinitions="*,34" ColumnSpacing="6">
                    <Slider
                        Minimum="8"
                        Maximum="22"
                        TickFrequency="0.5"
                        IsSnapToTickEnabled="True"
                        Value="{Binding UiFontSize, Mode=TwoWay}" />
                    <TextBlock Grid.Column="1" Classes="appearance-param-label" Text="{Binding UiFontSize, StringFormat='{}{0:0.#}'}" />
                  </Grid>
                </Grid>
                <Grid Grid.Column="1" RowDefinitions="Auto,Auto" RowSpacing="2" MinWidth="168">
                  <TextBlock Classes="field-label" Text="Code Editor size" />
                  <Grid Grid.Row="1" ColumnDefinitions="*,34" ColumnSpacing="6">
                    <Slider
                        Minimum="8"
                        Maximum="22"
                        TickFrequency="0.5"
                        IsSnapToTickEnabled="True"
                        Value="{Binding CodeEditorFontSize, Mode=TwoWay}" />
                    <TextBlock Grid.Column="1" Classes="appearance-param-label" Text="{Binding CodeEditorFontSize, StringFormat='{}{0:0.#}'}" />
                  </Grid>
                </Grid>
                <Grid Grid.Column="2" RowDefinitions="Auto,Auto" RowSpacing="2" MinWidth="168">
                  <TextBlock Classes="field-label" Text="Prompt Window size" />
                  <Grid Grid.Row="1" ColumnDefinitions="*,34" ColumnSpacing="6">
                    <Slider
                        Minimum="8"
                        Maximum="22"
                        TickFrequency="0.5"
                        IsSnapToTickEnabled="True"
                        Value="{Binding PromptWindowFontSize, Mode=TwoWay}" />
                    <TextBlock Grid.Column="1" Classes="appearance-param-label" Text="{Binding PromptWindowFontSize, StringFormat='{}{0:0.#}'}" />
                  </Grid>
                </Grid>
                <Grid Grid.Column="3" RowDefinitions="Auto,Auto" RowSpacing="2" MinWidth="168">
                  <TextBlock Classes="field-label" Text="Chat/ImageGen size" />
                  <Grid Grid.Row="1" ColumnDefinitions="*,34" ColumnSpacing="6">
                    <Slider
                        Minimum="8"
                        Maximum="22"
                        TickFrequency="0.5"
                        IsSnapToTickEnabled="True"
                        Value="{Binding ChatWindowFontSize, Mode=TwoWay}" />
                    <TextBlock Grid.Column="1" Classes="appearance-param-label" Text="{Binding ChatWindowFontSize, StringFormat='{}{0:0.#}'}" />
                  </Grid>
                </Grid>
              </Grid>
            </Border>

            <Border Grid.Row="2" Classes="rule-card" Padding="6,5">
              <Grid ColumnDefinitions="220,*" ColumnSpacing="10">
                <StackPanel Spacing="3">
                  <TextBlock Classes="field-label" Text="Chat Appearance" />
                  <ComboBox
                      x:Name="ChatAppearancePicker"
                      Classes="theme-picker"
                      ItemsSource="{Binding ChatAppearances}"
                      SelectedItem="{Binding SelectedChatAppearance, Mode=TwoWay}"
                      DropDownClosed="OnOptionPickerDropDownClosed"
                      SelectionChanged="OnChatAppearanceSelectionChanged">
                    <ComboBox.SelectionBoxItemTemplate>
                      <DataTemplate DataType="{x:Type vm:ThemeOptionViewModel}">
                        <TextBlock Text="{Binding Name}" FontSize="10.5" Foreground="{DynamicResource TextPrimaryBrush}" />
                      </DataTemplate>
                    </ComboBox.SelectionBoxItemTemplate>
                    <ComboBox.ItemTemplate>
                      <DataTemplate DataType="{x:Type vm:ThemeOptionViewModel}">
                        <StackPanel Spacing="0">
                          <TextBlock Text="{Binding Name}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" />
                          <TextBlock Text="{Binding Description}" FontSize="10" Foreground="{DynamicResource TextMutedBrush}" />
                        </StackPanel>
                      </DataTemplate>
                    </ComboBox.ItemTemplate>
                  </ComboBox>
                </StackPanel>

                <Grid Grid.Column="1" ColumnDefinitions="*,*,*" ColumnSpacing="6" MinHeight="70">
                  <Border
                      Grid.Column="0"
                      Padding="0"
                      CornerRadius="6"
                      Background="{DynamicResource ChatAssistantBubbleBrush}"
                      BorderBrush="{DynamicResource ChatAssistantBorderBrush}"
                      BorderThickness="1"
                      ClipToBounds="True">
                    <Grid RowDefinitions="17,*">
                      <Border Background="{DynamicResource ChatAssistantHeaderBrush}" BorderBrush="{DynamicResource ChatHeaderSeparatorBrush}" BorderThickness="0,0,0,1">
                        <Grid ColumnDefinitions="Auto,*,Auto" Margin="6,0" VerticalAlignment="Center">
                          <TextBlock Text="Codex" FontSize="8.5" FontWeight="Bold" Foreground="{DynamicResource ChatAssistantOwnerBrush}" />
                          <TextBlock Grid.Column="1" Text="CC" FontSize="7.5" Foreground="{DynamicResource ChatMetaBrush}" Margin="5,0,0,0" />
                          <TextBlock Grid.Column="2" Text="15:21" FontSize="7.5" FontWeight="Bold" Foreground="{DynamicResource ChatMetaBrush}" />
                        </Grid>
                      </Border>
                      <StackPanel Grid.Row="1" Margin="6,4" Spacing="3">
                        <TextBlock Text="request owner files" FontSize="8.5" Foreground="{DynamicResource ChatBodyTextBrush}" />
                        <Border Padding="5,2" CornerRadius="4" Background="{DynamicResource ChatSnippetShellBrush}" BorderBrush="{DynamicResource ChatSnippetBorderBrush}" BorderThickness="1">
                          <TextBlock Text="request" FontSize="7.5" Foreground="{DynamicResource ChatRequestBadgeTextBrush}" />
                        </Border>
                      </StackPanel>
                    </Grid>
                  </Border>

                  <Border
                      Grid.Column="1"
                      Padding="0"
                      CornerRadius="6"
                      Background="{DynamicResource ChatToolBubbleBrush}"
                      BorderBrush="{DynamicResource ChatToolBorderBrush}"
                      BorderThickness="1"
                      ClipToBounds="True">
                    <Grid RowDefinitions="17,*">
                      <Border Background="{DynamicResource ChatToolHeaderBrush}" BorderBrush="{DynamicResource ChatHeaderSeparatorBrush}" BorderThickness="0,0,0,1">
                        <Grid ColumnDefinitions="Auto,*,Auto" Margin="6,0" VerticalAlignment="Center">
                          <TextBlock Text="ccReplace" FontSize="8.5" FontWeight="Bold" Foreground="{DynamicResource ChatToolOwnerBrush}" />
                          <TextBlock Grid.Column="1" Text="GO preview" FontSize="7.5" Foreground="{DynamicResource ChatMetaBrush}" Margin="5,0,0,0" />
                          <TextBlock Grid.Column="2" Text="15:22" FontSize="7.5" FontWeight="Bold" Foreground="{DynamicResource ChatMetaBrush}" />
                        </Grid>
                      </Border>
                      <StackPanel Grid.Row="1" Margin="6,4" Spacing="3">
                        <TextBlock Text="Patch plan: 1 file" FontSize="8.5" Foreground="{DynamicResource ChatBodyTextBrush}" />
                        <Border Padding="5,2" CornerRadius="4" Background="{DynamicResource ChatSnippetShellBrush}" BorderBrush="{DynamicResource ChatSnippetBorderBrush}" BorderThickness="1">
                          <TextBlock Text="PromptComposer.axaml   +12  -4" FontSize="7.5" Foreground="{DynamicResource ChatMetaBrush}" />
                        </Border>
                      </StackPanel>
                    </Grid>
                  </Border>

                  <Border
                      Grid.Column="2"
                      Padding="0"
                      CornerRadius="6"
                      Background="{DynamicResource ChatUserBubbleBrush}"
                      BorderBrush="{DynamicResource ChatUserBorderBrush}"
                      BorderThickness="1"
                      ClipToBounds="True">
                    <Grid RowDefinitions="17,*">
                      <Border Background="{DynamicResource ChatUserHeaderBrush}" BorderBrush="{DynamicResource ChatHeaderSeparatorBrush}" BorderThickness="0,0,0,1">
                        <Grid ColumnDefinitions="Auto,*,Auto" Margin="6,0" VerticalAlignment="Center">
                          <TextBlock Text="You" FontSize="8.5" FontWeight="Bold" Foreground="{DynamicResource ChatUserOwnerBrush}" />
                          <TextBlock Grid.Column="1" Text="task" FontSize="7.5" Foreground="{DynamicResource ChatMetaBrush}" Margin="5,0,0,0" />
                          <TextBlock Grid.Column="2" Text="15:20" FontSize="7.5" FontWeight="Bold" Foreground="{DynamicResource ChatMetaBrush}" />
                        </Grid>
                      </Border>
                      <TextBlock Grid.Row="1" Margin="6,4" Text="make it cleaner" FontSize="8.5" Foreground="{DynamicResource ChatBodyTextBrush}" />
                    </Grid>
                  </Border>
                </Grid>
              </Grid>
            </Border>

            <Border Grid.Row="3" Classes="rule-card" Padding="4">
              <StackPanel Spacing="0">
                <TextBlock Classes="field-label" Text="Editor style" />
                <Grid ColumnDefinitions="*,*" Classes="editor-style-table">
                  <Border Classes="editor-style-cell left">
                    <StackPanel Spacing="0">
                      <Grid ColumnDefinitions="Auto,Auto" ColumnSpacing="12" MinHeight="17" HorizontalAlignment="Left">
                        <CheckBox
                            Classes="appearance-toggle"
                            Content="On/Off"
                            IsChecked="{Binding UseParentChildArrowIndentation, Mode=TwoWay}" />
                        <TextBlock Grid.Column="1" Classes="appearance-parent-label" Text="Parent/Child indentation" />
                      </Grid>
                      <Grid ColumnDefinitions="Auto,Auto" ColumnSpacing="12" MinHeight="17" HorizontalAlignment="Left">
                        <CheckBox
                            Classes="appearance-toggle"
                            Content="On/Off"
                            IsChecked="{Binding ShowVerticalScopeLines, Mode=TwoWay}" />
                        <TextBlock Grid.Column="1" Classes="appearance-parent-label" Text="Vertical line" />
                      </Grid>
                      <Grid ColumnDefinitions="Auto,Auto" ColumnSpacing="12" MinHeight="17" HorizontalAlignment="Left">
                        <CheckBox
                            Classes="appearance-toggle"
                            Content="On/Off"
                            IsChecked="{Binding UseColorfulFamilies, Mode=TwoWay}" />
                        <TextBlock Grid.Column="1" Classes="appearance-parent-label" Text="Colorful families" />
                      </Grid>
                      <Grid ColumnDefinitions="Auto,Auto" ColumnSpacing="12" MinHeight="17" HorizontalAlignment="Left">
                        <CheckBox
                            Classes="appearance-toggle"
                            Content="On/Off"
                            IsChecked="{Binding ShowAppearanceCodePreview, Mode=TwoWay}" />
                        <TextBlock Grid.Column="1" Classes="appearance-parent-label" Text="Code preview" />
                      </Grid>
                      <TextBlock Classes="appearance-param-label" Text="Theme adapt" Margin="0,5,0,1" />
                      <StackPanel Spacing="0" HorizontalAlignment="Left">
                        <Grid ColumnDefinitions="Auto,44" ColumnSpacing="10" MinHeight="17" HorizontalAlignment="Left">
                          <CheckBox
                              Classes="appearance-toggle"
                              Content="On/Off"
                              IsChecked="{Binding ThemeAdaptFileCountColor, Mode=TwoWay}" />
                          <TextBlock Grid.Column="1" Classes="appearance-parent-label" Text="F" />
                        </Grid>
                        <Grid ColumnDefinitions="Auto,44" ColumnSpacing="10" MinHeight="17" HorizontalAlignment="Left">
                          <CheckBox
                              Classes="appearance-toggle"
                              Content="On/Off"
                              IsChecked="{Binding ThemeAdaptLocColor, Mode=TwoWay}" />
                          <TextBlock Grid.Column="1" Classes="appearance-parent-label" Text="LOC" />
                        </Grid>
                        <Grid ColumnDefinitions="Auto,44" ColumnSpacing="10" MinHeight="17" HorizontalAlignment="Left">
                          <CheckBox
                              Classes="appearance-toggle"
                              Content="On/Off"
                              IsChecked="{Binding ThemeAdaptVersionColor, Mode=TwoWay}" />
                          <TextBlock Grid.Column="1" Classes="appearance-parent-label" Text="v" />
                        </Grid>
                        <Grid ColumnDefinitions="Auto,44" ColumnSpacing="10" MinHeight="17" HorizontalAlignment="Left">
                          <CheckBox
                              Classes="appearance-toggle"
                              Content="On/Off"
                              IsChecked="{Binding ThemeAdaptBytesColor, Mode=TwoWay}" />
                          <TextBlock Grid.Column="1" Classes="appearance-parent-label" Text="bytes" />
                        </Grid>
                      </StackPanel>
                    </StackPanel>
                  </Border>

                  <Border Grid.Column="1" Classes="editor-style-cell right">
                    <StackPanel Spacing="0">
                    <Grid ColumnDefinitions="Auto,Auto,Auto" ColumnSpacing="8" MinHeight="17" HorizontalAlignment="Left">
                      <Button
                          x:Name="SummaryArrowOptionsToggleButton"
                          Classes="summary-expander"
                          Content="v"
                          Click="OnSummaryArrowOptionsToggleClick" />
                      <CheckBox
                          Grid.Column="1"
                          Classes="appearance-toggle"
                          Content="On/Off"
                          IsChecked="{Binding ShowFoldArrows, Mode=TwoWay}" />
                      <TextBlock Grid.Column="2" Classes="appearance-parent-label" Text="Summary arrows" />
                    </Grid>
                    <Border
                        x:Name="SummaryArrowOptionsPanel"
                        Classes="summary-options-table"
                        IsEnabled="{Binding ShowFoldArrows}">
                      <StackPanel Spacing="0">
                        <Border Classes="summary-options-row first">
                          <Grid
                              ColumnDefinitions="78,150"
                              ColumnSpacing="8"
                              HorizontalAlignment="Stretch">
                            <TextBlock Classes="appearance-param-label" Text="Placement" />
                            <ComboBox
                                x:Name="FoldArrowPositionPicker"
                                Grid.Column="1"
                                Classes="theme-picker"
                                ItemsSource="{Binding FoldArrowPositions}"
                                SelectedItem="{Binding SelectedFoldArrowPosition, Mode=TwoWay}">
                              <ComboBox.SelectionBoxItemTemplate>
                                <DataTemplate DataType="{x:Type vm:ThemeOptionViewModel}">
                                  <TextBlock Text="{Binding Name}" FontSize="10.5" Foreground="{DynamicResource TextPrimaryBrush}" />
                                </DataTemplate>
                              </ComboBox.SelectionBoxItemTemplate>
                              <ComboBox.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:ThemeOptionViewModel}">
                                  <StackPanel Spacing="0">
                                    <TextBlock Text="{Binding Name}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" />
                                    <TextBlock
                                        Text="{Binding Description}"
                                        FontSize="10"
                                        Foreground="{DynamicResource TextMutedBrush}" />
                                  </StackPanel>
                                </DataTemplate>
                              </ComboBox.ItemTemplate>
                            </ComboBox>
                          </Grid>
                        </Border>

                        <Border Classes="summary-options-row">
                          <Grid
                              ColumnDefinitions="78,Auto"
                              ColumnSpacing="8"
                              MinHeight="17"
                              HorizontalAlignment="Stretch">
                            <TextBlock Classes="appearance-param-label" Text="Borders" />
                            <CheckBox
                                Grid.Column="1"
                                Classes="appearance-toggle"
                                Content="On/Off"
                                IsChecked="{Binding ShowSummaryArrowBorders, Mode=TwoWay}" />
                          </Grid>
                        </Border>

                        <Border Classes="summary-options-row last">
                          <Grid
                              ColumnDefinitions="78,*"
                              ColumnSpacing="8"
                              HorizontalAlignment="Stretch">
                            <TextBlock
                                Classes="appearance-param-label"
                                Text="Summarize:"
                                VerticalAlignment="Center" />
                            <Border Grid.Column="1" Classes="summary-kind-table">
                              <Grid ColumnDefinitions="2*,*,*">
                                <Border Classes="summary-kind-cell">
                                  <Grid RowDefinitions="Auto,*">
                                    <TextBlock
                                        Classes="summary-kind-category"
                                        Text="Curly braces { }"
                                        HorizontalAlignment="Center" />
                                    <Grid Grid.Row="1" ColumnDefinitions="Auto,Auto" ColumnSpacing="12" HorizontalAlignment="Center">
                                      <StackPanel Spacing="0">
                                        <CheckBox Classes="summary-kind-check" Content="namespace" IsChecked="{Binding SummarizeNamespace, Mode=TwoWay}" />
                                        <CheckBox Classes="summary-kind-check" Content="class" IsChecked="{Binding SummarizeClass, Mode=TwoWay}" />
                                        <CheckBox Classes="summary-kind-check" Content="struct" IsChecked="{Binding SummarizeStruct, Mode=TwoWay}" />
                                        <CheckBox Classes="summary-kind-check" Content="interface" IsChecked="{Binding SummarizeInterface, Mode=TwoWay}" />
                                        <CheckBox Classes="summary-kind-check" Content="enum" IsChecked="{Binding SummarizeEnum, Mode=TwoWay}" />
                                      </StackPanel>
                                      <StackPanel Grid.Column="1" Spacing="0">
                                        <CheckBox Classes="summary-kind-check" Content="method" IsChecked="{Binding SummarizeMethod, Mode=TwoWay}" />
                                        <CheckBox Classes="summary-kind-check" Content="property/accessor" IsChecked="{Binding SummarizeProperty, Mode=TwoWay}" />
                                        <CheckBox Classes="summary-kind-check" Content="object/initializer" IsChecked="{Binding SummarizeObject, Mode=TwoWay}" />
                                        <CheckBox Classes="summary-kind-check" Content="block" IsChecked="{Binding SummarizeBlock, Mode=TwoWay}" />
                                      </StackPanel>
                                    </Grid>
                                  </Grid>
                                </Border>

                                <Border Grid.Column="1" Classes="summary-kind-cell">
                                  <StackPanel Spacing="0">
                                    <TextBlock Classes="summary-kind-category" Text="Square brackets [ ]" HorizontalAlignment="Center" />
                                    <CheckBox Classes="summary-kind-check" Content="array/list" IsChecked="{Binding SummarizeArray, Mode=TwoWay}" />
                                  </StackPanel>
                                </Border>

                                <Border Grid.Column="2" Classes="summary-kind-cell last">
                                  <StackPanel Spacing="0">
                                    <TextBlock Classes="summary-kind-category" Text="Parentheses ( )" HorizontalAlignment="Center" />
                                    <CheckBox Classes="summary-kind-check" Content="arguments/group" IsChecked="{Binding SummarizeArguments, Mode=TwoWay}" />
                                  </StackPanel>
                                </Border>
                              </Grid>
                            </Border>
                        </Grid>
                      </Border>
                    </StackPanel>
                  </Border>
                    </StackPanel>
                  </Border>
                </Grid>
              </StackPanel>
            </Border>

            <Border Grid.Row="4" Classes="syntax-preview-card" Margin="0,2,0,0" ClipToBounds="True">
              <Grid RowDefinitions="Auto,*" RowSpacing="6" ClipToBounds="True">
                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                  <TextBlock Classes="field-label" Text="Preview" />
                  <TextBlock
                      x:Name="PreviewCaption"
                      Grid.Column="1"
                      Classes="preview-caption"
                      TextTrimming="CharacterEllipsis" />
                </Grid>
                <controls:CodeEditor
                    x:Name="AppearanceCodePreview"
                    Grid.Row="1"
                    CodeFontFamily="{Binding CodeFontFamily}"
                    EditorFontSize="{Binding CodeEditorFontSize}"
                    SkinKey="{Binding SkinKey}"
                    ShowMinimap="{Binding ShowAppearanceCodePreview}"
                    MinHeight="0"
                    ClipToBounds="True"
                    VerticalAlignment="Stretch"
                    ShowFoldArrows="{Binding ShowFoldArrows}"
                    ShowSummaryArrowBorders="{Binding ShowSummaryArrowBorders}"
                    SummaryFoldKinds="{Binding SummaryFoldKinds}"
                    FoldArrowsInCodeEditor="{Binding FoldArrowsInCodeEditor}"
                    UseParentChildArrowIndentation="{Binding EffectiveUseParentChildArrowIndentation}"
                    ShowVerticalScopeLines="{Binding ShowVerticalScopeLines}"
                    UseColorfulFamilies="{Binding UseColorfulFamilies}"
                    ShowFoldSummaryPreview="True" />
              </Grid>
            </Border>
          </Grid>
</UserControl>
